image: andthensome/alpine-node-bash

before_script:
    - chmod +x ci/install-browsers
    - sh ci/install-browsers

cache:
    paths:
        - node_modules/

test:
    script:
        - npm install
        - ./node_modules/@angular/cli/bin/ng test --browser=Chrome --single-run=true --watch=false

