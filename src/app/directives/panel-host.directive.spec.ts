import { PanelHostDirective } from './panel-host.directive';

describe('PanelHostDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelHostDirective();
    expect(directive).toBeTruthy();
  });
});
