import { DelayTooltipDirective } from './delay-tooltip.directive';

describe('DelayTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
