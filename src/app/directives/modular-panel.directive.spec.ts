import { ModularPanelDirective } from './modular-panel.directive';

describe('ModularPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new ModularPanelDirective();
    expect(directive).toBeTruthy();
  });
});
