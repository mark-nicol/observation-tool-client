export interface CoordSystem {
  sexagesimalLabels: {
    latLabel: string,
    lonLabel: string
  },
  normalLabels: {
    latLabel: string,
    lonLabel: string
  },
  latPlaceholder: string,
  lonPlaceholder: string,
  lonHeader: string,
  latHeader: string
}
