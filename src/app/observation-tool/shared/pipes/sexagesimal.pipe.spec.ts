import {SexagesimalPipe} from './sexagesimal.pipe';

describe('SexagesimalPipe', () => {
  it('create an instance', () => {
    const pipe = new SexagesimalPipe();
    expect(pipe).toBeTruthy();
  });
});
